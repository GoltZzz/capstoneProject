<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>S.F.A.S.</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
			crossorigin="anonymous" />
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
		<style>
			:root {
				--primary-color: #1a2a6c;
				--secondary-color: #b21f1f;
				--accent-color: #fdbb2d;
				--bg-gradient: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
				--text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
			}

			@keyframes fadeInUp {
				from {
					opacity: 0;
					transform: translateY(20px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes slideInLeft {
				from {
					opacity: 0;
					transform: translateX(-20px);
				}
				to {
					opacity: 1;
					transform: translateX(0);
				}
			}

			@keyframes slideInRight {
				from {
					opacity: 0;
					transform: translateX(20px);
				}
				to {
					opacity: 1;
					transform: translateX(0);
				}
			}

			@keyframes pulse {
				0% { transform: scale(1); }
				50% { transform: scale(1.05); }
				100% { transform: scale(1); }
			}

			body {
				height: 100vh;
				background: var(--bg-gradient);
				text-shadow: var(--text-shadow);
				font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
			}

			.cover-container {
				max-width: 60em;
				background: rgba(0, 0, 0, 0.2);
				backdrop-filter: blur(10px);
				border-radius: 20px;
				box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
			}

			header {
				opacity: 0;
				animation: slideInLeft 0.6s ease-out forwards;
				padding: 1rem;
			}

			.nav-masthead .nav-link {
				color: rgba(255, 255, 255, 0.8);
				border-bottom: 2px solid transparent;
				transition: all 0.3s ease;
				margin: 0 0.5rem;
				padding: 0.5rem 0;
			}

			.nav-masthead .nav-link:hover {
				color: white;
				border-bottom-color: var(--accent-color);
				transform: translateY(-2px);
			}

			.nav-masthead .nav-link.active {
				color: white;
				border-bottom-color: white;
			}

			main {
				opacity: 0;
				animation: fadeInUp 0.6s ease-out 0.3s forwards;
				padding: 3rem !important;
			}

			h1 {
				font-size: 3.5rem;
				font-weight: 700;
				margin-bottom: 1.5rem;
				background: linear-gradient(45deg, white, var(--accent-color));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				text-shadow: none;
			}

			.lead {
				font-size: 1.5rem;
				margin-bottom: 2rem;
				line-height: 1.6;
			}

			.emergency-contact {
				opacity: 0;
				animation: pulse 2s infinite, slideInRight 0.6s ease-out 0.6s forwards;
				background: rgba(178, 31, 31, 0.2);
				padding: 1rem 2rem;
				border-radius: 15px;
				border: 1px solid rgba(255, 255, 255, 0.1);
				margin: 2rem 0;
				display: inline-block;
			}

			.emergency-contact i {
				color: var(--accent-color);
				margin-right: 0.5rem;
			}

			.btn-custom {
				opacity: 0;
				animation: fadeInUp 0.6s ease-out 0.9s forwards;
				background: rgba(255, 255, 255, 0.1);
				color: white;
				border: 2px solid rgba(255, 255, 255, 0.2);
				padding: 0.8rem 2.5rem;
				font-size: 1.2rem;
				border-radius: 30px;
				transition: all 0.3s ease;
				margin: 0.5rem;
				backdrop-filter: blur(5px);
			}

			.btn-custom:hover {
				background: rgba(255, 255, 255, 0.2);
				border-color: white;
				transform: translateY(-3px);
				box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
				color: white;
			}

			footer {
				opacity: 0;
				animation: fadeInUp 0.6s ease-out 1.2s forwards;
				padding: 1rem;
				font-size: 0.9rem;
			}

			@media (max-width: 768px) {
				.cover-container {
					border-radius: 0;
				}

				h1 {
					font-size: 2.5rem;
				}

				.lead {
					font-size: 1.2rem;
				}

				.btn-custom {
					width: 100%;
					margin: 0.5rem 0;
				}
			}
		</style>
	</head>
	<body class="d-flex text-center text-white">
		<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
			<header class="mb-auto">
				<div>
					<h3 class="float-md-start mb-0">
						<i class="fas fa-shield-alt me-2"></i>S.F.A.S.
					</h3>
					<nav class="nav nav-masthead justify-content-center float-md-end">
						<a class="nav-link active" aria-current="page" href="#">
							<i class="fas fa-home me-1"></i>Home
						</a>
						<a class="nav-link" href="/sfas">
							<i class="fas fa-microchip me-1"></i>Devices
						</a>
						<% if (currentUser && currentUser.isAdmin) { %>
						<a class="nav-link" href="/admin/users">
							<i class="fas fa-user-shield me-1"></i>Admin Dashboard
						</a>
						<% } %> 
						<% if (currentUser) { %>
						<a class="nav-link" href="/logout">
							<i class="fas fa-sign-out-alt me-1"></i>Logout
						</a>
						<% }%>
					</nav>
				</div>
			</header>
			<main class="px-3">
				<% if (currentUser) { %>
				<h1>Welcome Back, <%= currentUser.username %>!</h1>
				<p class="lead">
					Thank you for choosing S.F.A.S. to protect what matters most.
					<br>
					Your safety is our priority.
				</p>
				<div class="emergency-contact">
					<i class="fas fa-phone-alt"></i>
					<strong>Emergency Hotline: 0917-500-8535</strong>
				</div>
				<% } else { %>
				<h1>Secure Fire Alert System</h1>
				<p class="lead">
					Welcome to S.F.A.S. - Your trusted partner in fire safety and property protection.
					<br>
					Advanced monitoring for peace of mind.
				</p>
				<div class="emergency-contact">
					<i class="fas fa-phone-alt"></i>
					<strong>Emergency Hotline: 0917-500-8535</strong>
				</div>
				<p class="text-white-50 mb-4">
					<i class="fas fa-info-circle me-2"></i>
					Please ensure you have a registered device before accessing the system
				</p>
				<% if (!currentUser) { %>
				<a href="/login" class="btn btn-custom">
					<i class="fas fa-sign-in-alt me-2"></i>Login to Dashboard
				</a>
				<% } %>
				<% } %>
			</main>
			<footer class="mt-auto text-white-50">
				<p>
					<i class="far fa-copyright me-1"></i>2024 S.F.A.S. | Protecting What Matters Most
				</p>
			</footer>
		</div>

		<script
			src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
			integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3"
			crossorigin="anonymous"></script>
		<script
			src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js"
			integrity="sha384-cuYeSxntonz0PPNlHhBs68uyIAVpIIOZZ5JqeqvYYIcEL727kskC66kF92t6Xl2V"
			crossorigin="anonymous"></script>
	</body>
</html>
